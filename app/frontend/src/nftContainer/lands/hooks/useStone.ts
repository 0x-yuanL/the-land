import { useEffect, useState } from "react";

const useUserStone = () => {
  /**
   * TODO: to use the user stone data fetched from the backend
   */
};

const useItemStone = () => {
  /**
   * TODO: to use the item stone data fetched from the backend
   */
};
