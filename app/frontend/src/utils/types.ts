export type Land = {
    name: string;
    address: string;
}
export type LandProp = {
    lastHarvestTimeStamp: number;
    owner: string;
    price: number;
}

export type LandUserProp = {
    avatar: string;
}